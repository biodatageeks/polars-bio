---
phase: 01-dataset-preparation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified: []
autonomous: false
requirements:
  - DATA-01
  - DATA-02
  - DATA-03

must_haves:
  truths:
    - "BAM file ENCFF588KDY.bam exists and samtools flagstat reports ~36M reads"
    - "BAM index ENCFF588KDY.bam.bai exists"
    - "GTF file gencode.v49.annotation.gtf exists and pb.scan_gtf() can read it"
    - "Chromosome names match between BAM and GTF (both chr-prefixed)"
    - "featureCounts reports version 2.1.1"
    - "htseq-count reports version 2.1.2"
  artifacts: []
  key_links: []
---

<objective>
User runs the notebook cells to download data (~3.2 GB BAM + ~1.6 GB GTF), install reference tools, and verify everything is ready for downstream phases.

Purpose: The notebook from Plan 01 is documentation -- this checkpoint confirms the data and tools actually exist on disk and pass validation.
Output: Validated dataset and tool environment ready for Phase 2 and Phase 3.
</objective>

<execution_context>
@/Users/mwiewior/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mwiewior/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-dataset-preparation/01-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run notebook data download and validation cells</name>
  <files>notebooks/feature_counting_benchmark.ipynb</files>
  <read_first>
    - notebooks/feature_counting_benchmark.ipynb
  </read_first>
  <action>
This is a human-verify checkpoint. The executor presents the notebook to the user for execution.

What was built: Benchmark notebook with all dataset preparation cells (created in Plan 01). The notebook downloads ENCODE BAM ENCFF588KDY (~3.2 GB), GENCODE v49 GTF (~1.6 GB uncompressed), indexes the BAM, validates chromosome naming consistency, and checks tool installations.

How to verify:
1. Open `notebooks/feature_counting_benchmark.ipynb` in Jupyter
2. Run all cells from top to bottom (Restart and Run All)
3. Verify the following outputs:
   - Section 2 (Config): prints the data directory path
   - Section 3 (BAM download): BAM file downloads successfully (~3.2 GB). MD5 matches `90779101e28e5b425be8e51310314b89`. Header shows `SO:coordinate`. `samtools flagstat` reports ~36M total reads. BAI index created.
   - Section 4 (GTF download): GTF downloads and decompresses. `pb.scan_gtf()` reports ~1.4M exon rows and ~60k unique gene_ids.
   - Section 5 (Chr naming): Prints BAM contigs starting with chr1, chr2, ... and GTF seqnames starting with chr1, chr2, ... Reports common contigs > 0. Prints "PASS: Chromosome naming is consistent".
   - Section 6 (Tools): `featureCounts -v` shows v2.1.1. `htseq-count --version` shows 2.1.2.
   - Section 7 (Summary): All files show "OK" status with non-zero sizes.
4. If any tool is not installed, follow the installation instructions in Section 6 markdown cells, then re-run the version check cell.

Resume signal: Type "approved" if all validations pass, or describe which cells failed and with what errors.
  </action>
  <verify>User confirms all notebook cells executed without errors and all validation checks passed.</verify>
  <acceptance_criteria>
    - samtools flagstat output contains "in total" with a number near 36000000
    - samtools view -H output contains "SO:coordinate"
    - ENCFF588KDY.bam.bai file exists at the data directory path
    - pb.scan_gtf() exon count is approximately 1400000
    - Chromosome naming validation cell prints "PASS"
    - featureCounts version output contains "2.1.1"
    - htseq-count version output contains "2.1.2"
  </acceptance_criteria>
  <done>User has approved: all data files exist, all validations pass, all tools installed with correct versions. Phase 1 is complete and downstream phases can proceed.</done>
</task>

</tasks>

<verification>
All verification happens during the human-verify checkpoint above. The user confirms:
1. BAM downloaded, sorted, indexed, ~36M reads
2. GTF downloaded, decompressed, readable by polars-bio
3. Chromosome names consistent (chr-prefixed)
4. featureCounts 2.1.1 and HTSeq-count 2.1.2 installed and responding
</verification>

<success_criteria>
- User has confirmed all notebook cells execute without errors
- ENCODE BAM and GENCODE GTF are on disk at the configured data directory
- Chromosome naming is verified consistent
- All three reference tools (featureCounts, HTSeq-count, samtools) respond with expected versions
</success_criteria>

<output>
After completion, create `.planning/phases/01-dataset-preparation/01-02-SUMMARY.md`
</output>
